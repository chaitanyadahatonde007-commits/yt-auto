# YouTube Data API v3 — API key (public read: search, videos, channels)
YOUTUBE_API_KEY=

# Optional OAuth (uploads, playlists you own, comments as you)
# Download OAuth Desktop client JSON from Google Cloud Console and set the path:
GOOGLE_OAUTH_CLIENT_SECRETS=
